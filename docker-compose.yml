version: "3.8"
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: badminton-manager:latest
    expose:
      - "4173"
    environment:
      - PORT=4173
    restart: unless-stopped
