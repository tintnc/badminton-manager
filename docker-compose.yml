services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: badminton-manager:latest
    ports:
      - "5173:5173"
    volumes:
      - ./data:/app/data:rw
    environment:
      - PORT=5173
    restart: unless-stopped
